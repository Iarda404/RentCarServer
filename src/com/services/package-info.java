/**
 * The service layer of the application, responsible for communication between the DAO and controllers.
 <p>
 *     Classes in package:
 *     <ul>
 *         <li>{@link com.services.UserService} - class that implements business logic and manage interactions between the DAO layer,
 *  *  and the controller layer, which handles user requests and responses</li>
 *         <li>{@link com.dao.CarDao} - class that implements business logic and manage interactions between the DAO layer,
 *  *  and the controller layer, which handles car requests and responses</li>
 *         <li>{@link com.dao.BookingDao} - class that implements business logic and manage interactions between the DAO layer,
 *  *  and the controller layer, which handles booking requests and responses</li>
 *      </ul>
 * </p>
 * @since 1.0
 * @version 1.0
 */
package com.services;